[package]
name = "buildlens-parser"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
buildlens-core = { path = "../buildlens-core" }
buildlens-diagnostics = { path = "../buildlens-diagnostics" }
buildlens-graph = { path = "../buildlens-graph" }
regex.workspace = true
serde.workspace = true
thiserror.workspace = true

[dev-dependencies]
criterion.workspace = true

[[bench]]
name = "parser"
harness = false
