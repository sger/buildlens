[package]
name = "buildlens-tests"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
buildlens-core = { path = "../buildlens-core" }
# The regexes live in buildlens-parser; this crate exposes them behind a
# trait rather than keeping a second copy that would drift.
buildlens-parser = { path = "../buildlens-parser" }
