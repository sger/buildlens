# Rust build output
/target/

# Backtrace / profiling artifacts
*.profraw
perf.data
perf.data.old
flamegraph.svg

# macOS
.DS_Store

# Editors
.idea/
*.swp

# Local secrets. compose reads BUILDLENS_TOKEN from here, so this file holds a
# real credential and must never be committed.
.env
.env.*
!.env.example
